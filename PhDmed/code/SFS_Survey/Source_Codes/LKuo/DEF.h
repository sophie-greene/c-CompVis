#define ARRAY_SIZE	256
#define ARRAY_SQ	(ARRAY_SIZE*ARRAY_SIZE)
#define DIM		0.1
#define EPSILON		1e-15
#define H		1.0
#define LAMBDA		2000.0
#define MaxGrids	10
#define THRESH		0.0001
#define max(n1, n2) ( ((n1) > (n2)) ? (n1) : (n2) )
#define min(n1, n2) ( ((n1) < (n2)) ? (n1) : (n2) )
